import { data } from "../view/doctor/profilelist/Profilelist"
// 获取医生信息
export const getDoctor = (key,callback)=>{
    if(key === null){

    }
}
export const getDoctors = (callback) => {
    callback(data);
}

// 获取医生排班表